<%= form_for([@post, @post.opinions.build]) do |f| %>
  <p>
  	<%= f.label :Nombre %><br>
  	<%= f.text_field :name %>
  </p>

  <p>
  	<%= f.label :Opinion %><br>
  	<%= f.text_area :body %>
  </p>

  <br>
  
  <p>
  	<%= f.submit %>
  </p>

<% end %>